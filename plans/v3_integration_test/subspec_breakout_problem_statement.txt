We are working in the repository `ekapadi/mantid`, on the branch `EWM15431_live-listener-interface__agents`. Our objective is to break-out the specification `plans/v3_integration_test/SNSListener-integration-test.md` + `plans/v3_integration_test/NN-....md` into a small series of sub-specs that are suitable for a coding-agent to implement. Implementing each subspec sequentially should then result in a series of commits to our single final PR for this integration test. We want to be able to point the agent at (e.g.) `overview-spec.md` + subspec03 and say "implement this". Unfortunately, how we've broken things out into subspecs right now won't do this -- the current subspecs each do discuss a separate set of important details about the overall implementation, but they are not implementation subspecs in the form that we need. Each implementation sub-spec should be largely stand-alone (, although it may reference a single primary spec), and it should be essentially a todo list. Note that we had already tried to implement this using the single overview spec, but the agent got confused -- all of the issues found by this first implementation attempt are already addressed by the discussion in the existing break-out subspecs. For this task, please work in a sub-directory `plans/v3_integration_test/subspecs`, and then when we have things as they need to be, we'll delete the previous subspecs so that the agent doesn't get confused. For this task, note that the details discussed in the present subspecs are important, so you need to be aware of the entire contents of the `plans/v3_integration_test` directory. The coding-agent restrictions in each "section 0" are also important, and they should be retained in each of the new subspecs.
